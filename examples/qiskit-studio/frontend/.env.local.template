# Qiskit Studio Frontend - LLM OS Edition
# Copy this file to .env.local and configure as needed

# =====================================================
# API Configuration - All endpoints point to unified backend
# =====================================================

# Chat endpoint (for Q&A and conversations)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/chat

# Parameter update endpoint (for code generation)
# Note: In LLM OS Edition, this uses the same /chat endpoint
NEXT_PUBLIC_PARAMETER_UPDATE_API_URL=http://127.0.0.1:8000/chat

# Code execution endpoint
NEXT_PUBLIC_RUNCODE_URL=http://127.0.0.1:8000/run

# =====================================================
# Debug Configuration
# =====================================================

# Debug mode - set to 'true' to enable debug logging in console
# Can also be controlled via localStorage.setItem('qiskit-studio-debug', 'true') in browser
NEXT_PUBLIC_DEBUG=false

# =====================================================
# Notes
# =====================================================
#
# Unlike the original qiskit-studio which uses 3 separate microservices:
# - chat-agent (port 8000)
# - codegen-agent (port 8001)
# - coderun-agent (port 8002)
#
# The LLM OS Edition runs a unified backend on port 8000 that handles
# all three functions with intelligent routing.
