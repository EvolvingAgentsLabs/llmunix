# LLMunix Memory Management Dependencies

# ZERO dependencies required for pure markdown architecture!
# All memory and trace storage uses pure markdown (headers + bold text)
# No YAML parsing, no database libraries, no external dependencies

# Optional utilities (if needed for specific projects)
python-dotenv>=1.0.0  # For environment variable management (optional)

# Wabi POC API Server Dependencies
fastapi>=0.104.0  # Web framework for API server
uvicorn>=0.24.0  # ASGI server
pydantic>=2.0.0  # Data validation
pyyaml>=6.0  # YAML parsing for user memory
