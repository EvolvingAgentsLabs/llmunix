# LLM-OS Core Dependencies
claude-agent-sdk>=0.1.0
anyio>=4.0.0
pyyaml>=6.0
numpy>=1.24.0

# Optional utilities
python-dotenv>=1.0.0  # For environment variable management

# Optional: API Server Dependencies (for examples)
fastapi>=0.104.0  # Web framework for API server
uvicorn>=0.24.0  # ASGI server
pydantic>=2.0.0  # Data validation

# Edge Runtime (optional, for edge_runtime/ folder)
# ollama  # Install via: pip install ollama
